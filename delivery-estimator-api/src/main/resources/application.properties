quarkus.http.port=${PORT:8097}

quarkus.application.name=delivery-estimator-api

# PostgreSQL
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USERNAME:ecommerce}
quarkus.datasource.password=${DB_PASSWORD:ecommerce}
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/ecommerce}

# Hibernate / Panache
quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=${HIBERNATE_LOG_SQL:false}
quarkus.hibernate-orm.database.default-schema=delivery

# Dev Services desligado porque usamos Docker Compose
quarkus.datasource.devservices.enabled=false

# OpenAPI / Swagger
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger-ui

# Health
quarkus.smallrye-health.root-path=/q/health

# Observability
quarkus.otel.enabled=${OTEL_ENABLED:true}
quarkus.otel.exporter.otlp.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}

# Metrics
quarkus.micrometer.export.prometheus.enabled=true

# Olist Delivery Baseline
app.olist-baseline.enabled=true
app.olist-baseline.resource=ml/olist_delivery_route_baseline.csv
app.olist-baseline.min-samples=5

# Delivery ML - Trained model loading
app.delivery-ml.enabled=true
app.delivery-ml.prefer-model=${APP_DELIVERY_ML_PREFER_MODEL:true}
app.delivery-ml.model-path=${APP_DELIVERY_ML_MODEL_PATH:/models/delivery/delivery-tribuo-v1.model}